// HOME 
const HOME = '/';
const ORDER_RANK = '/orderRank';
const ORDER_PICK = '/orderPick';

const routes = {
    home: HOME,
    order_rank: ORDER_RANK,
    order_pick: ORDER_PICK
}

export default routes;